🧠 # Интерпретатор Brainfuck (C++)
Минималистичная, но надёжная реализация интерпретатора эзотерического языка программирования Brainfuck на современном C++. Проект демонстрирует глубокое понимание управления памятью, обработки ошибок и точного следования формальной спецификации — без использования сторонних библиотек.

💡 Несмотря на всего 8 команд, Brainfuck является Тьюринг-полным языком. Этот интерпретатор показывает, что даже «игрушечные» языки требуют серьёзной инженерной дисциплины.

✨ Возможности
Полное соответствие канонической спецификации Brainfuck:
Лента из 30 000 ячеек, инициализированных нулями
Ячейки — беззнаковые 8-битные значения с автоматическим переносом (255 + 1 = 0)
Корректная обработка вложенных циклов [/]
Строгая проверка ошибок:
Выход указателя за границы ленты (< в самой левой ячейке или > за 30 000-й)
Непарные квадратные скобки
Некорректное поведение при вводе/выводе
Чёткий I/O: программа и входные данные читаются из файлов, результат выводится в stdout
Без зависимостей: написан на стандартном C++17, компилируется любым современным компилятором
Безопасная работа с памятью: лента размещена на стеке — нет выделения в куче, утечек или фрагментации
🛠️ Использование
bash
1
Интерпретатор ожидает:

<программа.bf> — исходный код на Brainfuck
<вход.txt> — входные данные (в виде байтов)
Результат выполнения программы выводится в stdout.
При любой ошибке времени выполнения (выход за границы ленты, несбалансированные скобки и т.д.) выводится ERROR, и программа завершается корректно.

🧪 Пример
hello.bf (выводит «Hello, World!»):

brainfuck
1
++++++++[>++++[>++>+++>+++>+<<<<-]>+>+>->>+[<]<-]>>.>---.+++++++..+++.>>.<-.<.+++.------.--------.>>+.>++.
Запуск:

bash
12
./bf hello.bf /dev/null
# Вывод: Hello, World!
💻 Технические особенности
Контроль над памятью: лента — статический массив unsigned char[30000], имитирующий ограниченную память реального устройства.
Эффективный поиск парных скобок: используется счётчик вложенности — O(n) на прыжок, без предварительного парсинга.
Автоматический перенос: благодаря типу unsigned char арифметика выполняется по модулю 256 «из коробки».
Защитное программирование: все граничные случаи (пустой ввод, неверные скобки, переполнение указателя) обрабатываются явно.
Бинарный ввод: файлы читаются в бинарном режиме — корректно обрабатываются любые байты, включая нулевые.
📦 Сборка
bash
1
Проект состоит из одного файла — не требуется CMake, Makefile или другие инструменты.